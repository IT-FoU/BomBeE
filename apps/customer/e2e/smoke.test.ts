import { describe, expect, it } from 'vitest';

/**
 * E2E placeholder for Milestone 0.
 * Real Playwright/browser flows land with Customer PWA (Milestone 11).
 */
describe('customer e2e placeholder', () => {
  it('keeps the e2e script wired for CI', () => {
    expect(true).toBe(true);
  });
});
